import 'package:flutter/material.dart';

class HealthInfo extends StatelessWidget {
  const HealthInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("건강정보 뷰")),
    );
  }
}